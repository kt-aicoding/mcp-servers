# MCP Servers 文档

这里保存 KT AI Coding 的 MCP 层资料：默认 MCP 面、CLI/MCP 边界、非默认 MCP 类型和 server 治理原则。

## 文档

| 文档 | 用途 |
| --- | --- |
| [本地 MCP 体系](local-mcp-system.md) | 默认 MCP 面、CLI/MCP 边界、非默认 MCP 类型和维护命令。 |

## 边界

这个目录用于保存 MCP 体系和治理资料。具体 server 的使用文档应放在 `servers/<server-name>/` 内，跟 server 实现一起维护。
